name: C89 CI

on:
  push:
    branches:
      - master
      - release/*

jobs:
  build:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make test
      run: make test
